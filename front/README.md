# React + Vite

to run project:
- cd front 
  - npm i
  - touch .env with var VITE_API_BASE_URL (usually = http://localhost:8000);
  - npm run dev
- cd portfolioBack
  - composer i
  - touch .env (or .env.dev or .env.local) with database connection data
  - symfony server:start
